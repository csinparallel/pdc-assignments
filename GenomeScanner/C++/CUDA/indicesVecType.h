#ifndef INDICESVECTYPE_H
#define INDICESVECTYPE_H 
// allows one to easily change the data type used for kernel.cu.

// adjust based on available GPU/memory storage (ex. int vs long)
typedef int indicesVecType;

#endif